{
  "name": "monorepo-demo",
  "version": "0.1.0",
  "private": true,
  "description": "EDM AI Helper of Netease",
  "license": "MIT",
  "scripts": {
    "start:next": "yarn workspace next dev",
    "start:vite": "yarn workspace vite dev",
    "start:koa": "yarn workspace koa dev",
    "build:next": "yarn workspace next build",
    "build:vite": "yarn workspace vite build"
  },
  "workspaces": [
    "packages/next",
    "packages/vite",
    "packages/koa",
    "packages/express"
  ],
  "devDependencies": {},
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
